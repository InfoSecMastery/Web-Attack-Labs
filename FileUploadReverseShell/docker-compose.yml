version: '3.8'

services:
  fileupload-app:
    build: .
    container_name: infosecmastery-fileupload
    ports:
      - "5001:80"
    volumes:
      - uploads_data:/var/www/html/uploads
    restart: unless-stopped

volumes:
  uploads_data: